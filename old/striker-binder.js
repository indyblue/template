'use strict';
(function () {
	/* todo:
			- track back through dataPath to get path
			- for arrays, need a key, (sort/add/remove)
			- anyplace with remove, add comment tags before/after
			- store deep copy of data
			- write comparator for data/deep copy to apply necessary changes
	*/
})();
